# civil_engineering — v1.0 — schema layer

This directory holds the schema definition only.

- `civil_engineering.yaml` — pack manifest (26 types, 5 spaces)
- `types/` — 26 type YAML files with facet axes (topic_material, topic_property, topic_phase, topic_scope, topic_environment)

Ingest this folder first so the OpenCRAB extractor learns the schema before ingesting raw text in v1.1+.
